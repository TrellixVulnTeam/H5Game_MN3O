class DailySignMonthRewardConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<DailySignMonthRewardConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly id:number;    public readonly item_id:number;    public readonly item_num:number;    public constructor(id : number , item_id : number , item_num : number ){		super()        this.id = id;        this.item_id = item_id;        this.item_num = item_num;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "DailySignMonthRewardConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 3)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("DailySignMonthRewardConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {               var id: number  = rs.ReadInt();      var item_id: number  = rs.ReadInt();      var item_num: number  = rs.ReadInt();      var  new_obj_DailySignMonthRewardConfig : DailySignMonthRewardConfig = new DailySignMonthRewardConfig(id, item_id, item_num);                if(this.ContainsKey(id))        {            return;        }		this.dic[id]= new_obj_DailySignMonthRewardConfig;        this.array.push(new_obj_DailySignMonthRewardConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="DailySignMonthRewardConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):DailySignMonthRewardConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}