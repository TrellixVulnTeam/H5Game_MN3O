class BlackScreenConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<BlackScreenConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly id:number;    public readonly nextId:number;    public readonly content:string;    public constructor(id : number , nextId : number , content : string ){		super()        this.id = id;        this.nextId = nextId;        this.content = content;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "BlackScreenConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 3)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("BlackScreenConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {               var id: number  = rs.ReadInt();      var nextId: number  = rs.ReadInt();      var content: string  = rs.ReadString();      var  new_obj_BlackScreenConfig : BlackScreenConfig = new BlackScreenConfig(id, nextId, content);                if(this.ContainsKey(id))        {            return;        }		this.dic[id]= new_obj_BlackScreenConfig;        this.array.push(new_obj_BlackScreenConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="BlackScreenConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):BlackScreenConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}