class FaceConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<FaceConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly id:number;    public readonly player_icon:string;    public readonly face_icon:string[];    public readonly face_location:number[];    public readonly face_size:number[];    public constructor(id : number , player_icon : string , face_icon : string[] , face_location : number[] , face_size : number[] ){		super()        this.id = id;        this.player_icon = player_icon;        this.face_icon = face_icon;        this.face_location = face_location;        this.face_size = face_size;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "FaceConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 5)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("FaceConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {                     var arr_item_len_FaceConfig:number;      var id: number  = rs.ReadInt();      var player_icon: string  = rs.ReadString();        arr_item_len_FaceConfig = rs.ReadShort();    var  face_icon :  string[] = [];        for(var i = 0; i < arr_item_len_FaceConfig; ++i)            face_icon[i] = rs.ReadString();        arr_item_len_FaceConfig = rs.ReadShort();    var  face_location :  number[] = [];        for(var i = 0; i < arr_item_len_FaceConfig; ++i)            face_location[i] = rs.ReadInt();        arr_item_len_FaceConfig = rs.ReadShort();    var  face_size :  number[] = [];        for(var i = 0; i < arr_item_len_FaceConfig; ++i)            face_size[i] = rs.ReadInt();      var  new_obj_FaceConfig : FaceConfig = new FaceConfig(id, player_icon, face_icon, face_location, face_size);                if(this.ContainsKey(id))        {            return;        }		this.dic[id]= new_obj_FaceConfig;        this.array.push(new_obj_FaceConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="FaceConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):FaceConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}