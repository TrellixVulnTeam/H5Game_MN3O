class CreatureSpawnConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<CreatureSpawnConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly id:number;    public readonly templet:number;    public readonly level:number;    public readonly attrMethod:number;    public readonly attrID:number;    public constructor(id : number , templet : number , level : number , attrMethod : number , attrID : number ){		super()        this.id = id;        this.templet = templet;        this.level = level;        this.attrMethod = attrMethod;        this.attrID = attrID;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "CreatureSpawnConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 5)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("CreatureSpawnConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {               var id: number  = rs.ReadInt();      var templet: number  = rs.ReadInt();      var level: number  = rs.ReadInt();      var attrMethod: number  = rs.ReadInt();      var attrID: number  = rs.ReadInt();      var  new_obj_CreatureSpawnConfig : CreatureSpawnConfig = new CreatureSpawnConfig(id, templet, level, attrMethod, attrID);                if(this.ContainsKey(id))        {            return;        }		this.dic[id]= new_obj_CreatureSpawnConfig;        this.array.push(new_obj_CreatureSpawnConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="CreatureSpawnConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):CreatureSpawnConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}