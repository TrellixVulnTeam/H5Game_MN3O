class BuffEffectConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<BuffEffectConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly id:number;    public readonly desc:string;    public readonly type:number;    public readonly para1:number;    public readonly para2:number;    public readonly para3:number;    public readonly para4:number;    public readonly para5:number;    public readonly para6:number;    public readonly para1_lvmod:string;    public readonly para2_lvmod:string;    public readonly para3_lvmod:string;    public readonly para4_lvmod:string;    public readonly para5_lvmod:string;    public readonly para6_lvmod:string;    public constructor(id : number , desc : string , type : number , para1 : number , para2 : number , para3 : number , para4 : number , para5 : number , para6 : number , para1_lvmod : string , para2_lvmod : string , para3_lvmod : string , para4_lvmod : string , para5_lvmod : string , para6_lvmod : string ){		super()        this.id = id;        this.desc = desc;        this.type = type;        this.para1 = para1;        this.para2 = para2;        this.para3 = para3;        this.para4 = para4;        this.para5 = para5;        this.para6 = para6;        this.para1_lvmod = para1_lvmod;        this.para2_lvmod = para2_lvmod;        this.para3_lvmod = para3_lvmod;        this.para4_lvmod = para4_lvmod;        this.para5_lvmod = para5_lvmod;        this.para6_lvmod = para6_lvmod;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "BuffEffectConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 15)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("BuffEffectConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {               var id: number  = rs.ReadInt();      var desc: string  = rs.ReadString();      var type: number  = rs.ReadInt();      var para1: number  = rs.ReadInt();      var para2: number  = rs.ReadInt();      var para3: number  = rs.ReadInt();      var para4: number  = rs.ReadFloat();      var para5: number  = rs.ReadFloat();      var para6: number  = rs.ReadFloat();      var para1_lvmod: string  = rs.ReadString();      var para2_lvmod: string  = rs.ReadString();      var para3_lvmod: string  = rs.ReadString();      var para4_lvmod: string  = rs.ReadString();      var para5_lvmod: string  = rs.ReadString();      var para6_lvmod: string  = rs.ReadString();      var  new_obj_BuffEffectConfig : BuffEffectConfig = new BuffEffectConfig(id, desc, type, para1, para2, para3, para4, para5, para6, para1_lvmod, para2_lvmod, para3_lvmod, para4_lvmod, para5_lvmod, para6_lvmod);                if(this.ContainsKey(id))        {            return;        }		this.dic[id]= new_obj_BuffEffectConfig;        this.array.push(new_obj_BuffEffectConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="BuffEffectConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):BuffEffectConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}