class BuffGroupConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<BuffGroupConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly id:number;    public readonly oper:number;    public constructor(id : number , oper : number ){		super()        this.id = id;        this.oper = oper;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "BuffGroupConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 2)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("BuffGroupConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {               var id: number  = rs.ReadInt();      var oper: number  = rs.ReadInt();      var  new_obj_BuffGroupConfig : BuffGroupConfig = new BuffGroupConfig(id, oper);                if(this.ContainsKey(id))        {            return;        }		this.dic[id]= new_obj_BuffGroupConfig;        this.array.push(new_obj_BuffGroupConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="BuffGroupConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):BuffGroupConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}