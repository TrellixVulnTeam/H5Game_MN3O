class WechatConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<WechatConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly id:number;    public readonly personID:number;    public readonly days:number;    public readonly head:string;    public readonly name:string;    public readonly contentId:number[];    public readonly pic:string;    public readonly talks:number[];    public readonly friendliness:number[];    public readonly otherReply:number[];    public readonly type:number;    public readonly param1:number;    public readonly param2:number;    public readonly param3:number;    public readonly param4:number;    public readonly param5:number;    public constructor(id : number , personID : number , days : number , head : string , name : string , contentId : number[] , pic : string , talks : number[] , friendliness : number[] , otherReply : number[] , type : number , param1 : number , param2 : number , param3 : number , param4 : number , param5 : number ){		super()        this.id = id;        this.personID = personID;        this.days = days;        this.head = head;        this.name = name;        this.contentId = contentId;        this.pic = pic;        this.talks = talks;        this.friendliness = friendliness;        this.otherReply = otherReply;        this.type = type;        this.param1 = param1;        this.param2 = param2;        this.param3 = param3;        this.param4 = param4;        this.param5 = param5;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "WechatConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 16)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("WechatConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {                     var arr_item_len_WechatConfig:number;      var id: number  = rs.ReadInt();      var personID: number  = rs.ReadInt();      var days: number  = rs.ReadInt();      var head: string  = rs.ReadString();      var name: string  = rs.ReadString();        arr_item_len_WechatConfig = rs.ReadShort();    var  contentId :  number[] = [];        for(var i = 0; i < arr_item_len_WechatConfig; ++i)            contentId[i] = rs.ReadInt();      var pic: string  = rs.ReadString();        arr_item_len_WechatConfig = rs.ReadShort();    var  talks :  number[] = [];        for(var i = 0; i < arr_item_len_WechatConfig; ++i)            talks[i] = rs.ReadInt();        arr_item_len_WechatConfig = rs.ReadShort();    var  friendliness :  number[] = [];        for(var i = 0; i < arr_item_len_WechatConfig; ++i)            friendliness[i] = rs.ReadInt();        arr_item_len_WechatConfig = rs.ReadShort();    var  otherReply :  number[] = [];        for(var i = 0; i < arr_item_len_WechatConfig; ++i)            otherReply[i] = rs.ReadInt();      var type: number  = rs.ReadInt();      var param1: number  = rs.ReadInt();      var param2: number  = rs.ReadInt();      var param3: number  = rs.ReadInt();      var param4: number  = rs.ReadFloat();      var param5: number  = rs.ReadFloat();      var  new_obj_WechatConfig : WechatConfig = new WechatConfig(id, personID, days, head, name, contentId, pic, talks, friendliness, otherReply, type, param1, param2, param3, param4, param5);                if(this.ContainsKey(id))        {            return;        }		this.dic[id]= new_obj_WechatConfig;        this.array.push(new_obj_WechatConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="WechatConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):WechatConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}