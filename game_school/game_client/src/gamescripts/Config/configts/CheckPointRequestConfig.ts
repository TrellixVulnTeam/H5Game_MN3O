class CheckPointRequestConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<CheckPointRequestConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly id:number;    public readonly type:number;    public readonly para1:number;    public readonly para2:number;    public readonly para3:number;    public readonly number:number;    public readonly describe:string;    public constructor(id : number , type : number , para1 : number , para2 : number , para3 : number , number : number , describe : string ){		super()        this.id = id;        this.type = type;        this.para1 = para1;        this.para2 = para2;        this.para3 = para3;        this.number = number;        this.describe = describe;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "CheckPointRequestConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 7)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("CheckPointRequestConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {               var id: number  = rs.ReadInt();      var type: number  = rs.ReadInt();      var para1: number  = rs.ReadInt();      var para2: number  = rs.ReadFloat();      var para3: number  = rs.ReadFloat();      var number: number  = rs.ReadInt();      var describe: string  = rs.ReadString();      var  new_obj_CheckPointRequestConfig : CheckPointRequestConfig = new CheckPointRequestConfig(id, type, para1, para2, para3, number, describe);                if(this.ContainsKey(id))        {            return;        }		this.dic[id]= new_obj_CheckPointRequestConfig;        this.array.push(new_obj_CheckPointRequestConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="CheckPointRequestConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):CheckPointRequestConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}