class ChatGroupNumConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<ChatGroupNumConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly chatGroupID:number;    public readonly groupHead:string;    public readonly name:string;    public constructor(chatGroupID : number , groupHead : string , name : string ){		super()        this.chatGroupID = chatGroupID;        this.groupHead = groupHead;        this.name = name;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "ChatGroupNumConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 3)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("ChatGroupNumConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {               var chatGroupID: number  = rs.ReadInt();      var groupHead: string  = rs.ReadString();      var name: string  = rs.ReadString();      var  new_obj_ChatGroupNumConfig : ChatGroupNumConfig = new ChatGroupNumConfig(chatGroupID, groupHead, name);                if(this.ContainsKey(chatGroupID))        {            return;        }		this.dic[chatGroupID]= new_obj_ChatGroupNumConfig;        this.array.push(new_obj_ChatGroupNumConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="ChatGroupNumConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):ChatGroupNumConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}