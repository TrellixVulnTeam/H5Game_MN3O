class GuideConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<GuideConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly id:number;    public readonly closeLevel:number;    public readonly triggerType:number;    public readonly triggerKey:number;    public readonly countdown:number;    public readonly endType:number;    public readonly para1:number;    public readonly para2:number;    public readonly type:number;    public readonly ui:number;    public readonly itemName:string;    public readonly arrow:number;    public readonly des:string;    public readonly desPos:number;    public constructor(id : number , closeLevel : number , triggerType : number , triggerKey : number , countdown : number , endType : number , para1 : number , para2 : number , type : number , ui : number , itemName : string , arrow : number , des : string , desPos : number ){		super()        this.id = id;        this.closeLevel = closeLevel;        this.triggerType = triggerType;        this.triggerKey = triggerKey;        this.countdown = countdown;        this.endType = endType;        this.para1 = para1;        this.para2 = para2;        this.type = type;        this.ui = ui;        this.itemName = itemName;        this.arrow = arrow;        this.des = des;        this.desPos = desPos;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "GuideConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 14)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("GuideConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {               var id: number  = rs.ReadInt();      var closeLevel: number  = rs.ReadInt();      var triggerType: number  = rs.ReadInt();      var triggerKey: number  = rs.ReadInt();      var countdown: number  = rs.ReadInt();      var endType: number  = rs.ReadInt();      var para1: number  = rs.ReadInt();      var para2: number  = rs.ReadInt();      var type: number  = rs.ReadInt();      var ui: number  = rs.ReadInt();      var itemName: string  = rs.ReadString();      var arrow: number  = rs.ReadInt();      var des: string  = rs.ReadString();      var desPos: number  = rs.ReadInt();      var  new_obj_GuideConfig : GuideConfig = new GuideConfig(id, closeLevel, triggerType, triggerKey, countdown, endType, para1, para2, type, ui, itemName, arrow, des, desPos);                if(this.ContainsKey(id))        {            return;        }		this.dic[id]= new_obj_GuideConfig;        this.array.push(new_obj_GuideConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="GuideConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):GuideConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}