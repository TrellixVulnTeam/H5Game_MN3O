class WechatContentConfig extends BaseConfig{    public static  resLoaded:boolean = false;    private static  dic: Object = {};  	public static array: Array<WechatContentConfig> = [];    public static  Init()    {       this. LoadRes();    }    public readonly id:number;    public readonly type:number;    public readonly name:string;    public readonly choiceContent:string;    public readonly content:string;    public readonly replyContent:string;    public constructor(id : number , type : number , name : string , choiceContent : string , content : string , replyContent : string ){		super()        this.id = id;        this.type = type;        this.name = name;        this.choiceContent = choiceContent;        this.content = content;        this.replyContent = replyContent;    }     private static  OnLoadFile( data:any)    { 		var byteData:egret.ByteArray = new egret.ByteArray( data );		var rs:ReadStream = new ReadStream(byteData);        /*int file_len = */rs.ReadInt();        var flag :string= rs.ReadString();        if(flag != "WechatContentConfig")        {            return;        }        var col_cnt:number = rs.ReadShort();        if(col_cnt != 6)        {            return;        }        var row_cnt:number = rs.ReadInt();        for( var i = 0; i < row_cnt; i++)        {          this.  Add_Item(rs);        }	  ConfFact.SetResLoad("WechatContentConfig");        this.  resLoaded = true;    }     private static  Add_Item(rs:ReadStream )    {               var id: number  = rs.ReadInt();      var type: number  = rs.ReadInt();      var name: string  = rs.ReadString();      var choiceContent: string  = rs.ReadString();      var content: string  = rs.ReadString();      var replyContent: string  = rs.ReadString();      var  new_obj_WechatContentConfig : WechatContentConfig = new WechatContentConfig(id, type, name, choiceContent, content, replyContent);                if(this.ContainsKey(id))        {            return;        }		this.dic[id]= new_obj_WechatContentConfig;        this.array.push(new_obj_WechatContentConfig);    }	private static  LoadRes()    {        if(this.resLoaded) return;		 if(this.isUseLocalTable)        {        var urlName:string = this. bytespath+="WechatContentConfig.bytes";        RES.getResByUrl(urlName,this.OnLoadFile,this,RES.ResourceItem.TYPE_BIN);        }            }	 public static  GetConfig(  id ):WechatContentConfig    {        for (var key in this.dic)            {            if(key==id)            {                 return this.dic[key];               }        }        return null;    }		   public static  ContainsKey(  id ):boolean    {        for (var key in this.dic)            {            if(key==id)            {                 return true;            }        }        return false;    }}