----------------------------------------------------------
-- Npc的特别掉落：预留的脚本掉落接口，暂时未用
----------------------------------------------------------

function Drop1(dropper)
	-- dropper:GetModDrop():AddToDropList(100001,666)
end
